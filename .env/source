#!/bin/bash

ENVDIR="$( cd "$( dirname "$0" )" && pwd )"
COMPONENT=$1
SCOPE=$ENVDIR/$COMPONENT

# Source env.zsh from each component or from supplied component
for envfile in $(find $SCOPE -name env.zsh); do
  source $envfile
done
